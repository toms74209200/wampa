linters:
  enable:
    - gofmt
    - goimports
    - gosimple
    - govet
    - staticcheck

run:
  deadline: 5m

issues:
  max-issues-per-linter: 0
  max-same-issues: 0

linters-settings:
  gofmt:
    simplify: true
  goimports:
    local-prefixes: wampa